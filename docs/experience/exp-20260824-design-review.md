# 经验总结 - 2026-08-24 - 设计文档审查（气泡皮肤与设置重组）

## 基本信息
- 技能名称：auto-task-experience-summarizer（审查类任务）
- 执行时间：2026-08-24 03:53
- 项目上下文：dsh-chat-focus v0.2.5（E:\dsh-chat-focus）

## 关键步骤
1. 通读 docs/design/ui-design-20260817-dsh-chat-focus-气泡皮肤与设置重组/ 全部 7 份文档。
2. 对总规划 §1.2/§9 声称的「代码事实」逐条抽查源码行号与标识符。
3. 按内部一致性、技术可行性、安全、发版切分四个维度输出分级问题清单。

## 坑是什么 → 怎么避 → 相关工具/文件 → 统一原则

| 坑是什么 | 怎么避 | 相关工具/文件 | 统一原则 |
|----------|--------|--------------|----------|
| PowerShell `Measure-Object -Line` 管道统计 Get-Content 输出时**不含空行**，845 行的文件只报 813，差点据此误判设计文档「845 行」声明失实 | 统计物理行数用 `(Get-Content $f).Count` 或直接读文件末尾行号；引用行数结论前先用文件真实末行交叉验证 | `pwsh`、`src/client/settings/ChatFocusSection.tsx` | 计数类 cmdlet 有隐式过滤语义，作为「证伪证据」前必须换一种方法复核 |
| 经验体系三文档（docs/experience/index.md、docs/design/参考文档索引.md、docs/help/developer-handbook.md）在本仓库尚不存在，技能前置读取必然失败 | 先 glob 确认存在性再读取；缺失时在产出中注明并继续任务，不虚构其内容 | `glob`、`docs/experience/index.md` | 前置检查先验证前提物存在性，缺失是事实输入而非错误 |
| 设计文档 §9 曾登记「六模块已于 v0.4.0 实现」，与工作树不符后整体回退——文档状态声明不可直接采信 | 审查时对「实现状态/代码事实」类声明抽样到源码行号级核对（本次抽查全部命中） | `grep`、`read`、`ui-design-20260817-...-总规划.md` §9 | 文档中的事实性断言要落到可复验的文件:行号锚点 |
| 跨 6 份文档落地 4 项用户决策后，旧术语（localStorage、v0.5.0、「废弃移除」）散留在历史段落/失败场景表/兼容表中，极易漏改 | 多文件修订完成后必须用 grep 反查全部被替换术语，逐一区分「有意保留（历史记录/拒绝理由）」与「真实残留」再收尾 | `grep`、`docs/design/ui-design-20260817-.../` 各模块文档 | 批量文档修订的完成标志不是改完最后一处，而是反查零残留 |

## 优化建议
- 设计文档群后续修订时，把「发版里程碑 × 模块行为」做成一张矩阵表，避免 M4 决策 4.4 与 v0.4.0/v0.5.0 切分这类跨文档时序矛盾。
- localStorage 降级层容量、zip 解压安全等平台约束，建议在设计模板中设为固定自查项。

## 本次可沉淀结论
- 该设计文档群的代码事实锚点（截至 v0.2.5）经抽查全部准确，可作为后续实施的可靠基线；主要风险集中在两段式应用的发版切分与降级层容量假设。
